import { Route } from '@angular/router';
import { CartDetailComponent } from './cart-detail/cart-detail.component';

export const cartDetailRoutes: Route[] = [
  { path: '', component: CartDetailComponent },
];
